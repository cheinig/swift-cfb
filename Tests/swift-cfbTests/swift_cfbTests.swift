import XCTest
@testable import swift_cfb

class swift_cfbTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct results.
//        XCTAssertEqual(swift_cfb().text, "Hello, World!")
    }


    static var allTests : [(String, (swift_cfbTests) -> () throws -> Void)] {
        return [
            ("testExample", testExample),
        ]
    }
}
